# AI Meeting Summarizer - Python Dependencies
# Install with: pip install -r requirements.txt

# --- Web Framework ---
flask>=3.0.0
flask-cors>=4.0.0

# --- Transcription ---
# Whisper requires ffmpeg to be installed on the system (not via pip)
# Install ffmpeg: https://ffmpeg.org/download.html
openai-whisper @ git+https://github.com/openai/whisper.git

# --- ML / Embeddings ---
torch>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# --- HTTP (Ollama API calls) ---
requests>=2.31.0

# --- Tokenization (used in chunk_creation notebook) ---
nltk>=3.8.0
tiktoken>=0.5.0
